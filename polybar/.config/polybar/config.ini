[bar/example]
width = 100%
height = 14pt
radius = 8
padding = 1

background = ${colors.background}
foreground = ${colors.foreground}

line-size = 3pt

module-margin = 1

border-size = 4pt
border-color = #00000000

font-0 = GeistMono Nerd Font Mono;2

modules-left = xworkspaces xwindow
modules-right = pulseaudio memory netspeed wlan eth date

cursor-click = pointer
cursor-scroll = ns-resize


enable-ipc = true
[module/xworkspaces]
type = internal/xworkspaces

label-active = %name%
label-active-background = ${colors.primary}
label-active-padding = 2

label-occupied = %name%
label-occupied-padding = 1

label-urgent = %name%
label-urgent-background = ${colors.alert}
label-urgent-padding = 1

label-empty = %name%
label-empty-foreground = ${colors.disabled}
label-empty-padding = 1

[module/xwindow]
type = internal/xwindow
format-prefix = "󱂬 "
format-prefix-foreground = ${colors.primary}
label = %title:0:60:...%

[module/pulseaudio]
type = internal/pulseaudio

format-volume-prefix = " "
format-volume-prefix-foreground = ${colors.primary}
format-volume = <label-volume>

label-volume = %percentage%%

label-muted = muted
label-muted-foreground = ${colors.disabled}

[module/memory]
type = internal/memory
interval = 2
format-prefix = " "
format-prefix-foreground = ${colors.primary}
label = %percentage_used:2%%

[network-base]
type = internal/network
interval = 5
format-prefix = "󰛳 "
format-prefix-foreground = ${colors.primary}
format-connected = <label-connected>
format-disconnected = <label-disconnected>
label-disconnected = %{F#F0C674}%ifname%%{F#707880} disconnected

[module/wlan]
inherit = network-base
interface = wlan0

format-prefix = " "
format-prefix-foreground = ${colors.primary}

label-connected = %{F#F0C674}%essid%%{F-} %local_ip%

[module/mobile]
inherit = network-base
interface = ccmni0

format-prefix = "󰩟 "
format-prefix-foreground = ${colors.primary}

label-connected = %{F#F0C674}mobile%{F-} %local_ip%

[module/eth]
inherit = network-base
interface-type = wired
format-prefix = " "
format-prefix-foreground = ${colors.primary}
label-connected = %{F#F0C674}%ifname%%{F-} %local_ip%

[module/date]
type = internal/date
interval = 1

format-prefix = "󰥔 "
format-prefix-foreground = ${colors.primary}
date = %I:%M
date-alt = %Y-%m-%d %I:%M:%S

label = %date%
label-foreground = ${colors.primary}

[settings]
screenchange-reload = true
pseudo-transparency = true

include-file = "~/.cache/wal/colors-polybar.ini"
